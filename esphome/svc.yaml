apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: hass
  name: esphome
  namespace: hass
spec:
  selector:
    app.kubernetes.io/name: hass
  ports:
  - name: esphome
    port: 6052
    targetPort: 6052
  type: ClusterIP
