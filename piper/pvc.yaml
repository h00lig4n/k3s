apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: piper
  namespace: hass
  labels:
    app: piper
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 10Gi
