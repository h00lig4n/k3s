apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: searxng
  namespace: ai
  labels:
    app: searxng
    app.kubernetes.io/component: websearch
    app.kubernetes.io/name: tsearxnga
    app.kubernetes.io/part-of: ai
spec:
  scaleTargetRef:
    name: searxng
  minReplicaCount: 0
  maxReplicaCount: 1
  cooldownPeriod: 60
  triggers:
    - type: kubernetes-workload
      metadata:
        podSelector: app=openwebui
        value: "1"