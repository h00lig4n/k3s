apiVersion: v1
kind: Service
metadata:
  name: signal
  namespace: common
  labels:
    app.kubernetes.io/name: signal
spec:
  type: ClusterIP
  selector:
    app: signal
  ports:
    - port: 8080
      targetPort: 8080
