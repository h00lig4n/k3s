apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  scopes: '[permissionNames]'
  policy.csv: |
    g, admin, role:admin
    g, editor, role:editor
    g, viewer, role:readonly