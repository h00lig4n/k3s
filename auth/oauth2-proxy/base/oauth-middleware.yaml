apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: oauth-headers
  namespace: auth
spec:
  headers:
    sslRedirect: true
    stsSeconds: 31536000
    stsIncludeSubdomains: true
    stsPreload: true
    forceSTSHeader: true