apiVersion: v1
kind: Service
metadata:
  name: vector
  labels:
    app.kubernetes.io/component: ingest
    app.kubernetes.io/part-of: logging
    app.kubernetes.io/name: vector
spec:
  type: ClusterIP
  selector:
    app: vector
  ports:
    - name: udp-logs
      port: 5140
      targetPort: 5140
      protocol: UDP
