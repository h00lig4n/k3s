apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openwebui
  namespace: ai
  labels:
    app: openwebui
    app.kubernetes.io/component: gui
    app.kubernetes.io/part-of: ai
    app.kubernetes.io/name: openwebui
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openwebui-longhorn
  namespace: ai
  labels:
    app: openwebui
    app.kubernetes.io/component: gui
    app.kubernetes.io/part-of: ai
    app.kubernetes.io/name: openwebui
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi